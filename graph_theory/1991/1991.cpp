#include <iostream>
using namespace std;

struct Node {
	char value;
	Node* left;
	Node* right;
};

int n;
int main(void) {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	Node* tree = new Node({'A', nullptr, nullptr});
	

	return (0);
}