#include <iostream>
#include <string>
using namespace std;

struct Accum {
	string s;
	int
string s1, s2;
string table[41][41];
int result = 0;

int main(void) {
	cin >> s1 >> s2;
	s1.insert(0, "X");
	s2.insert(0, "X");

	for (int y = 1; y < s1.length(); y++) {
		for (int x = 1; x < s2.length(); x++) {
			
		}
	}
	cout << result;

	return (0);
}